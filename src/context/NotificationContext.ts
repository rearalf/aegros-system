import { createContext } from 'react'
import { defaulNotificationContext } from '../Interface/NorificationInterface'

const NotificationContext = createContext(defaulNotificationContext)

export default NotificationContext
